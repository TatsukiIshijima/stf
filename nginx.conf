worker_processes auto;

events {
  worker_connections 1024;
}

http {
  keepalive_timeout   65;
  types_hash_max_size 2048;

  default_type        application/octet-stream;

  upstream stf_app {
    server app:7105 max_fails=0;
  }

  upstream stf_auth {
    server auth:7120 max_fails=0;
  }

  upstream stf_storage_apk {
    server storage-plugin-apk:7104 max_fails=0;
  }

  upstream stf_storage_image {
    server storage-plugin-image:7103 max_fails=0;
  }

  upstream stf_storage {
    server storage-temp:7102 max_fails=0;
  }

  upstream stf_websocket {
    server websocket:7110 max_fails=0;
  }

  upstream stf_api {
    server api:7106 max_fails=0;
  }

  types {
    application/javascript  js;
    image/gif               gif;
    image/jpeg              jpg;
    text/css                css;
    text/html               html;
  }

  map $http_upgrade $connection_upgrade {
    default  upgrade;
    ''       close;
  }

  server {
    listen        80 default_server;
    listen        [::]:80 default_server;
    server_tokens off;
    root          /dev/null;

    # Dockerでは内蔵DNSサーバが起動するらしく、127.0.0.11で固定のようなので固定値としている
    # 動的に対応したい場合はnginxコンテナ起動時にDNSサーバーのアドレスを取得し、
    # IPアドレスを置き換えた以下の内容(resolver IPアドレス)をresolver.confに書き込む
    resolver 127.0.0.11 valid=300s;
    resolver_timeout 10s;

    location ~ "^/d/nuc/([^/]+)/(?<port>[0-9]{3,5})/$" {
      proxy_pass http://provider:$port/;
      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection $connection_upgrade;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Real-IP $remote_addr;
    }

    location /auth/ {
      proxy_pass http://stf_auth/auth/;
    }

    location /api/ {
      proxy_pass http://stf_api/api/;
    }

    location /s/image/ {
      proxy_pass http://stf_storage_image;
    }

    location /s/apk/ {
      proxy_pass http://stf_storage_apk;
    }

    location /s/ {
      client_max_body_size 1024m;
      client_body_buffer_size 128k;
      proxy_pass http://stf_storage;
    }

    location /socket.io/ {
      proxy_pass http://stf_websocket;
      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection $connection_upgrade;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Real-IP $http_x_real_ip;
    }

    location / {
      proxy_pass http://stf_app;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Real-IP $http_x_real_ip;
    }
  }
}
